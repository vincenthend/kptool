import * as React from 'react'
import { ModalProps } from 'antd'

interface Props extends ModalProps {}

function SettingsModal(props: Props): JSX.Element {
  return <div></div>
}

export default SettingsModal
